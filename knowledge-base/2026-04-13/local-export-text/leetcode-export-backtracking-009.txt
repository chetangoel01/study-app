!mcq! { "description": "Backtracking is an optimization on:", "answer": 2, "choices": [ { "description": "Greedy" }, { "description": "DFS and BFS" }, { "description": "Exhaustive search" }, { "description": "Recursion" } ], "explanation": "Instead of generating all candidates and checking them, backtracking abandons a path once it can no longer lead to answers." } !mcq! !mcq! { "description": "If a problem is meant to be solved with backtracking, the expected range of n is:", "answer": 0, "choices": [ { "description": "n <= 15" }, { "description": "15 < n <= 100" }, { "description": "100 < n <= 1000" }, { "description": "1000 < n <= 10000" }, { "description": "n > 10000" } ], "explanation": "Backtracking algorithms typically have a exponential time complexity or worse." } !mcq! !mcq! { "description": "Backtracking is faster than exhaustive search because:", "answer": 1, "choices": [ { "description": "It uses recursion" }, { "description": "It can eliminate many candidates at a time" }, { "description": "The arguments are passed by reference" }, { "description": "It isn't" } ], "explanation": "Backtracking doesn't wait until the end to check if answers are valid." } !mcq! !mcq! { "description": "The space of candidates being built-into solutions can be visualized as a:", "answer": 2, "choices": [ { "description": "Matrix" }, { "description": "Undirected graph" }, { "description": "Tree" }, { "description": "Array" } ], "explanation": "A valid solution is like a leaf node." } !mcq! Consider the following code, where nums is an integer array: The following is a list of questions for you to answer. Click on each question to reveal the answer. What does the function "fn" return? fn returns all subsets of nums . What are some parts of the code that indicate this? We are adding to ans at every function call. There is no criteria for adding to ans , we add at every call. We aren't allowing duplicates by using the i argument. Consider the line of code with the comment "A". Other than in C++, why don't we just add "curr" to the answer? Why do we have extra code? curr is passed by reference, so only one version of curr exists at any given time. In this line, we are creating a copy of curr to add to ans . If we didn't create a copy and simply added curr , we wouldn't be adding the value of curr to the answer, but a reference to it. Thus, our answer would simply consist of many references to curr , and as curr is empty at the end of the algorithm, we would be left with many empty arrays in our answer. Consider the line of code with the comment "B". What would happen to the algorithm if we deleted it? As mentioned in the answer to the previous question, there is only one version of curr as it is passed by reference. If we removed this line, we would not be "undoing" any changes, and curr would grow to very large sizes, with many duplicates. Why are we using an argument "i" in the backtrack function? Why don't we just start our for loop from 0? The purpose of the i argument is to avoid duplicates. When we add nums[j] , we pass j + 1 to the next call. Because we start iterating from i , this ensures that we will only consider elements in the array that come after the one we just added. If we just started our for loop at j = 0 , the code would never terminate. We would add nums[0] to curr and then call backtrack again infinitely.
