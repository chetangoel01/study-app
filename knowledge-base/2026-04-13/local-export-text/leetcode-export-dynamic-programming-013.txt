!mcq! { "description": "Usually, top-down algorithms are faster than bottom-up ones.", "answer": 1, "choices": [ { "description": "True" }, { "description": "False" } ], "explanation": "Usually, a bottom-up algorithm will be faster than the equivalent top-down algorithm as there is some overhead with recursive function calls." } !mcq! !mcq! { "description": "The three main components of the framework are:", "answer": [1, 2, 5], "checkbox": true, "choices": [ { "description": "The choice between bottom-up and top-down" }, { "description": "The base cases" }, { "description": "The recurrence relation" }, { "description": "The number of dimensions" }, { "description": "The time complexity" }, { "description": "A data structure and a way of visiting each DP state" } ], "explanation": "Commit these to memory! Understanding the components of the framework allows us to approach dynamic programming problems in a step by step way." } !mcq! !mcq! { "description": "Which of the following require state variables?", "answer": [0, 2, 3], "checkbox": true, "choices": [ { "description": "Remaining number of moves allowed" }, { "description": "Original length of the input" }, { "description": "Current index along the input" }, { "description": "Number of keys currently being held" }, { "description": "The original number of moves allowed" } ], "explanation": "Constants should never be state variables." } !mcq! !mcq! { "description": "We can reduce space complexity when:", "answer": 1, "choices": [ { "description": "We are using top-down and not bottom-up" }, { "description": "The recurrence relation only involves a fixed number of states" }, { "description": "Only when we have more than 1 state variable" } ], "explanation": "Usually, reductions involve only temporarily caching results to subproblems. Once we know that the result of a subproblem will never be used again, we can discard it. This is a common optimization in bottom-up implementations. Because we iterate over all of the states in an orderly manner, we can determine which cached results will not be used again based on the recurrence relation and the current state." } !mcq! !mcq! { "description": "Without memoization, a recursive solution for Fibonacci Number would have the time complexity:", "answer": 2, "choices": [ { "description": "O(n)" }, { "description": "O(log n)" }, { "description": "O(2^n)" }, { "description": "O(n^2)" } ], "explanation": "Each call to F(n) makes 2 additional calls, to F(n - 1) and F(n - 2). Those 2 calls will then generate 4 calls, which will generate 8, etc." } !mcq!
