!mcq! { "description": "Which of the following is true about the above graph?", "answer": [0, 1, 4], "checkbox": true, "choices": [ { "description": "The sum of all indegrees is 5." }, { "description": "The graph is directed." }, { "description": "The graph has multiple connected components." }, { "description": "The graph is acyclic." }, { "description": "The sum of all outdegrees is 5." } ], "explanation": "The Graphs article of this chapter can be used to refresh your memory on graph terminology." } !mcq! !mcq! { "description": "If you performed a BFS on the above graph starting from node 0, which node will be visited last?", "answer": 0, "choices": [ { "description": "5" }, { "description": "7" }, { "description": "2" }, { "description": "8" } ], "explanation": "With breadth first search, every node is visited in the minimum steps possible. The last node visited will be the node that is the farthest from the origin." } !mcq! !mcq! { "description": "Which of the following are common formats for graphs?", "answer": [0, 1, 2, 5], "checkbox": true, "choices": [ { "description": "Matrix" }, { "description": "Adjacency list" }, { "description": "Array of edges" }, { "description": "Adjacency counter" }, { "description": "Edge tracker" }, { "description": "Adjacency matrix" } ], "explanation": "These are all listed in the Graphs article." } !mcq! !mcq! { "description": "In an undirected graph, a node's outdegree is equal to the number of neighbors it has.", "answer": 0, "choices": [ { "description": "True" }, { "description": "False" } ], "explanation": "In an undirected graph, every edge that touches a node contributes to it's outdegree, and it must connect to a neighbor." } !mcq! !mcq! { "description": "Another term for node is:", "answer": 1, "choices": [ { "description": "Edge" }, { "description": "Vertex" }, { "description": "Link" }, { "description": "State" }, { "description": "None of the above" } ], "explanation": "Plural: vertices." } !mcq! Consider the following code: The following is a list of questions for you to answer. Click on each question to reveal the answer. What input format does isConnected represent? isConnected is an adjacency matrix. isConnected[i][j] means that there is an edge from i to j . In this problem, are the input graphs directed or undirected? How can you tell? The graphs are undirected. We can tell because when we build the graph data structure, we add both j to graph[i] and i to graph[j] . What does the function "fn" return? fn returns the size of the largest connected component. How can you tell? What is the process? In the dfs function, we initialize ans = 1 at each node . We then iterate over each neighbor and if neighbor is not yet visited, we call dfs(neighbor) and add the result to ans . At the end, we return ans . Each node will contribute 1 toward ans when we initialize it. Any initial dfs call will visit the entire connected component, so the initial dfs call will return the size of the connected component. We take the maximum of these. What modifications would be necessary if we instead wanted to find the size of the smallest connected component? We would need to initialize ans (in the fn scope) to a large value like infinity. Then, we would update ans with dfs(i) using the min function instead of the max function. Why are we building a new graph data structure out of isConnected? While not mandatory, it is very convenient for us to have a data structure where we can find all the neighbors of any given node . With isConnected (an adjacency matrix), we would need to iterate over an entire row of the matrix for a given node to find its neighbors. Consider the line of code with the comment "A". What would happen if we deleted this line? A call to dfs may give us a result that is 1 greater than it should be. In the initial call to dfs(i) , we set ans = 1 , which indicates that node i is contributing 1 toward the size. If i has any neighbors, we would move to them. From any of those neighbors, because i was never added to seen , we would visit i again. Thus, we double-count i when it comes to the component's size. Consider the line of code with the comment "B". What would happen if we instead initialized the variable to 0? dfs would always return 0 , because there is no other place for ans to be incremented. Consider the line of code with the comment "C". What would happen if we deleted this line? If the graph had any edges, we would end up in an infinite loop. For example, let's say there was an edge A <-> B . We could move from A to B then back to A then back to B and so on forever, because we are no longer marking nodes that have already been visited.
