!mcq! { "description": "A post-order DFS prioritizing left over right will handle the nodes in what order?", "answer": 1, "choices": [ { "description": "6, 7, 1, 9, 2, 3" }, { "description": "6, 1, 7, 9, 2, 3" }, { "description": "9, 2, 1, 6, 7, 3" }, { "description": "3, 7, 6, 1, 2, 9" } ], "explanation": "Left nodes will be handled until there are none, then right nodes, then finally the original node." } !mcq! !mcq! { "description": "An in-order DFS prioritizing left over right will handle the nodes in what order?", "answer": 0, "choices": [ { "description": "6, 7, 1, 3, 2, 9" }, { "description": "6, 1, 7, 9, 2, 3" }, { "description": "9, 2, 1, 6, 7, 3" }, { "description": "3, 7, 6, 1, 2, 9" } ], "explanation": "Left nodes will be handled until there are none, then the original node, then right nodes until there are none." } !mcq! !mcq! { "description": "What traversal will handle the nodes of a binary search tree in sorted order?", "answer": 2, "choices": [ { "description": "Pre-order, left -> right" }, { "description": "Post-order, left -> right" }, { "description": "In-order, left -> right" }, { "description": "Post-order, right -> left" }, { "description": "Pre-order, right -> left" }, { "description": "In-order, right -> left" } ], "explanation": "First handle the left nodes, then the original node, then the right nodes." } !mcq! !mcq! { "description": "If A is the parent of B, and C is in the subtree rooted at B, then C is in the subtree rooted at A.", "answer": 0, "choices": [ { "description": "True" }, { "description": "False" } ], "explanation": "If C is in the subtree rooted at B, then B is an ancestor of C, and A is an ancestor of B." } !mcq! !mcq! { "description": "If A is the parent of B and C, D is the parent of E and F, and X is the parent of A and D, which of the following is true?", "answer": [0, 3, 4], "checkbox": true, "choices": [ { "description": "B, C, E, and F are all at the same depth." }, { "description": "For any subtree, if E is in it, then so is F." }, { "description": "The subtree rooted at X has at least 8 nodes." }, { "description": "If the described nodes are the only nodes that exist, then there are 4 leaves." }, { "description": "The subtree rooted at X contains at least all nodes mentioned." } ], "explanation": "Try drawing the tree out on paper to help visualize it. " } !mcq! !mcq! { "description": "In regard to implementing iteratively with a stack, which traversal is easiest to code?", "answer": 3, "choices": [ { "description": "BFS" }, { "description": "Post-order DFS" }, { "description": "In-order DFS" }, { "description": "Pre-order DFS" } ], "explanation": "In-order and post-order DFS are much trickier to implement iteratively than pre-order." } !mcq! Consider the following code: The following is a list of questions for you to answer. Click on each question to reveal the answer. This function takes a binary tree node and an integer as input. What does it return? This function returns the number of nodes that have a value equal to x . Consider the line of code with the comment "A". What would happen if we changed it so that the answer was always initialized to 1? This line is determining if the current node has a value equal to x , and thus if it should contribute toward the answer. If we changed this line so that ans was always initialized as 1 , every node in the tree would contribute toward the answer, regardless of its value. Thus, the function would instead count how many nodes are in the tree. What if we changed it so the answer was always initialized to 0? In that case, no node would contribute towards the answer, and the function will always return 0 . Notice that this line is the only place where ans can become non-zero. Why do we need to check for the base case? If we don't explicitly check the base case, then eventually root will be null, and upon trying to access attributes like val, left, right , the code will throw an error since null doesn't have those attributes. Why do we return 0 in the base case? The base case is when root = null , which implies we have an empty tree (0 nodes). Thus, it is guaranteed that there are 0 nodes with a value equal to x , so we simply return 0 . This function is implementing DFS. How would you implement BFS to solve the same problem? Initialize a queue with the root node and the answer variable ans . While queue is not empty: Remove a node from the front of queue . If node.val == x , increment ans . Add the children of node to queue if they exist. Return ans . Typically, binary search trees (BST) do not allow duplicate values. Why do you think this is? The BST property has no ambiguity. Lesser values go left, and greater values go right. If you allow duplicate values, it raises the question: do equal values go left or right? Beyond the scope of this course, there are many algorithms that would break if duplicate values were allowed, for example, common implementations of red-black trees. In the context of LeetCode, most problems would still be solvable if duplicates were allowed with minimal additional effort.
