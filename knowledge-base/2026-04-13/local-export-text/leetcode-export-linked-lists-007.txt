!mcq! { "description": "You have a node called head representing the list 1 -> 2 -> 3 -> 4 -> 5. You do head.next.next.next = None. What is the list now?", "answer": 1, "choices": [ { "description": "1 -> 2 -> 3 -> 5" }, { "description": "1 -> 2 -> 3" }, { "description": "1 -> 2 -> 3 -> 4" } ], "explanation": "On assignments, everything to the left of the final .next is one node." } !mcq! !mcq! { "description": "What is the biggest disadvantage of linked lists compared to arrays?", "answer": 2, "choices": [ { "description": "You need a pointer for constant time arbitrary removals." }, { "description": "Iterating through a linked list is more expensive." }, { "description": "There is no random access." }, { "description": "Linked lists don't have fixed sizes." } ], "explanation": "In many algorithm problems where the input can be thought of as an abstract list, indexing is extremely important." } !mcq! !mcq! { "description": "Let's say you have a singly linked list, and a pointer at position i. You want to delete the node at position i - 1. Let the time complexity of this operation be x. Now, say you have a doubly linked list in the same situation, let the time complexity be y. By what factor is y faster than x?", "answer": 0, "choices": [ { "description": "O(n)" }, { "description": "O(1), they're the same." }, { "description": "O(log n)" }, { "description": "O(n), but only if i is near the end of the list." } ], "explanation": "Remember that in terms of big O, constants are ignored. So even if i is only 1% of the length, it's still linear. The time complexity is better with a doubly linked list because you can use the 'prev' pointer to access the node at position i - 1." } !mcq! !mcq! { "description": "Select all useful concepts for solving linked list problems.", "answer": [0, 3, 4, 5], "checkbox": true, "choices": [ { "description": "Dummy node" }, { "description": "Never using recursion" }, { "description": "Converting into an array" }, { "description": "A 'fast' pointer" }, { "description": "Pointer for the previous node" }, { "description": "Sentinel nodes" } ], "explanation": "These 4 concepts maintain elegance." } !mcq! Consider the following code: The following is a list of questions for you to answer. Click on each question to reveal the answer. There is a small issue with the code. Ignoring the issue for now, what do you think this code is trying to do? This code implements an algorithm for determining if a linked list has a cycle. What is the issue? We should be checking for fast.next as well in the while loop. Why? In the while loop, we are accessing fast.next.next . If fast is at the final node, then fast.next will be null, which means fast.next.next is equivalent to null.next . As null does not have a next attribute, the code would throw an error.
